# Chá de Casa Nova — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a static housewarming gift registry site for Douglas & Keila, hosted on GitHub Pages, with gift reservations persisted in Supabase.

**Architecture:** Multi-file static site (HTML + CSS + Vanilla JS). `config.js` is the single source of truth for all editable content. Supabase stores only the `reservas` table with `UNIQUE(presente_id)` to prevent double-booking. Supabase Realtime pushes reservation events to all open browser sessions in real time.

**Tech Stack:** HTML5, CSS3, Vanilla JS (ES6+), Supabase JS v2 (CDN), Google Fonts (Great Vibes + Lato), GitHub Pages

---

## File Map

| File | Responsibility |
|------|---------------|
| `index.html` | Full page structure — all sections and modal scaffolding |
| `styles.css` | All styling — CSS variables, typography, layout, responsive |
| `config.js` | All editable content — couple info, gifts, schedule, Pix |
| `app.js` | All logic — render, Supabase client, modal, form, Realtime, Pix copy |
| `assets/casal/` | Couple photos (placeholder until real photos provided) |
| `assets/presentes/` | Gift photos (placeholder until real photos provided) |

---

### Task 1: Project scaffold

**Files:**
- Create: `index.html`
- Create: `styles.css`
- Create: `config.js`
- Create: `app.js`
- Create: `.gitignore`
- Create: `assets/casal/.gitkeep`
- Create: `assets/presentes/.gitkeep`

- [ ] **Step 1: Create .gitignore**

```
.superpowers/
.DS_Store
Thumbs.db
```

- [ ] **Step 2: Create asset directories**

```bash
mkdir -p assets/casal assets/presentes
touch assets/casal/.gitkeep assets/presentes/.gitkeep
```

- [ ] **Step 3: Create index.html**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chá de Casa Nova — Douglas & Keila</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body>

  <section id="hero">
    <h1 class="couple-name" id="couple-name"></h1>
    <p class="event-title">Chá de Casa Nova</p>
    <div class="event-details">
      <span id="event-date"></span>
      <span class="dot">·</span>
      <span id="event-time"></span>
    </div>
    <p class="event-address" id="event-address"></p>
    <p class="invite-message" id="invite-message"></p>
  </section>

  <section id="historia">
    <div class="section-inner two-col">
      <img src="assets/casal/casal.jpg" alt="Douglas e Keila" class="section-photo"
        onerror="this.style.display='none'">
      <p class="section-text" id="historia-text"></p>
    </div>
  </section>

  <section id="trocadilho">
    <div class="section-inner two-col reverse">
      <p class="trocadilho-text" id="trocadilho-text"></p>
      <img src="assets/casal/fome.jpg" alt="Douglas faminto" class="section-photo"
        onerror="this.style.display='none'">
    </div>
  </section>

  <section id="programacao">
    <h2 class="section-title">Programação do Dia</h2>
    <div class="programacao-grid" id="programacao-grid"></div>
  </section>

  <section id="presentes">
    <h2 class="section-title">Lista de Presentes</h2>
    <p class="section-subtitle">Escolha um presentinho e faça parte desse momento especial</p>
    <div class="presentes-grid" id="presentes-grid"></div>
  </section>

  <section id="pix">
    <h2 class="section-title">Quer contribuir com um Pix?</h2>
    <p class="section-subtitle">Se preferir, nos presenteie com uma contribuição</p>
    <div class="pix-container">
      <div class="qrcode-placeholder">QR Code</div>
      <div class="pix-info">
        <p class="pix-label">Telefone</p>
        <p class="pix-value" id="pix-phone"></p>
        <p class="pix-label">Chave Pix</p>
        <p class="pix-value" id="pix-key"></p>
        <button class="btn-copy" id="btn-copy-pix">Copiar chave</button>
        <p class="copy-feedback hidden" id="copy-feedback">✓ Copiado!</p>
      </div>
    </div>
  </section>

  <footer id="footer">
    <p class="footer-couple" id="footer-couple"></p>
    <p class="footer-thanks">Com muito amor, aguardamos vocês.</p>
  </footer>

  <!-- Modal de reserva -->
  <div class="modal-overlay hidden" id="modal-overlay">
    <div class="modal">
      <button class="modal-close" id="modal-close" aria-label="Fechar">×</button>
      <img class="modal-gift-img" id="modal-gift-img" src="" alt=""
        onerror="this.style.display='none'">
      <h3 class="modal-gift-name" id="modal-gift-name"></h3>
      <p class="modal-gift-price" id="modal-gift-price"></p>
      <form id="reservation-form" novalidate>
        <label class="field-label">
          Seu nome completo
          <input type="text" id="field-name" required placeholder="Maria da Silva">
        </label>
        <label class="field-label">
          WhatsApp
          <input type="tel" id="field-whatsapp" required placeholder="(11) 99999-9999">
        </label>
        <fieldset class="color-fieldset">
          <legend>Cor que você vai presentear</legend>
          <div class="color-options" id="color-options">
            <button type="button" class="color-circle" data-color="cinza-claro"
              style="background:#D3D3D3" title="Cinza claro" aria-label="Cinza claro"></button>
            <button type="button" class="color-circle" data-color="inox"
              style="background:#8D8D8D" title="Inox" aria-label="Inox"></button>
            <button type="button" class="color-circle" data-color="off-white"
              style="background:#FAF8F5;border:1px solid #ccc" title="Off-white" aria-label="Off-white"></button>
          </div>
          <input type="hidden" id="field-color">
        </fieldset>
        <p class="form-error hidden" id="form-error"></p>
        <button type="submit" class="btn-primary" id="btn-submit">Confirmar reserva</button>
      </form>
      <div class="reservation-success hidden" id="reservation-success">
        <p class="success-emoji">🏠</p>
        <p class="success-message" id="success-message"></p>
      </div>
    </div>
  </div>

  <script src="config.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 4: Create empty styles.css**

```css
/* styles.css — populated in Task 2 */
```

- [ ] **Step 5: Create empty config.js**

```js
// config.js — populated in Task 3
const CONFIG = {}
```

- [ ] **Step 6: Create empty app.js**

```js
// app.js — populated progressively
```

- [ ] **Step 7: Verify HTML loads in browser**

Open `index.html` in a browser (double-click the file).
Expected: blank off-white page, no console errors, Google Fonts network request visible in DevTools Network tab.

- [ ] **Step 8: Commit**

```bash
git add index.html styles.css config.js app.js .gitignore assets/
git commit -m "feat: project scaffold"
```

---

### Task 2: CSS — full styling

**Files:**
- Modify: `styles.css`

- [ ] **Step 1: Write full styles.css**

```css
/* ── Variables ──────────────────────────────────── */
:root {
  --color-bg:           #FAF8F5;
  --color-gold:         #C9A84C;
  --color-olive:        #6B7C5A;
  --color-olive-light:  #8A9E77;
  --color-card-bg:      #FFFFFF;
  --color-border:       #EDE8DF;
  --color-muted:        #9E9689;

  --font-cursive: 'Great Vibes', cursive;
  --font-body:    'Lato', sans-serif;

  --section-pad: 5rem 1.5rem;
  --max-w:       900px;
  --radius:      12px;
}

/* ── Reset ──────────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
img { max-width: 100%; display: block; }
button { cursor: pointer; border: none; background: none; font-family: inherit; }

/* ── Base ───────────────────────────────────────── */
body {
  font-family: var(--font-body);
  background: var(--color-bg);
  color: var(--color-olive);
  line-height: 1.7;
}

/* ── Utility ────────────────────────────────────── */
.hidden { display: none !important; }

.section-inner {
  max-width: var(--max-w);
  margin: 0 auto;
}

.section-title {
  font-family: var(--font-cursive);
  font-size: clamp(2rem, 5vw, 3rem);
  color: var(--color-gold);
  text-align: center;
  margin-bottom: 0.5rem;
}

.section-subtitle {
  text-align: center;
  color: var(--color-muted);
  font-size: 0.95rem;
  margin-bottom: 3rem;
}

/* ── Hero ───────────────────────────────────────── */
#hero {
  text-align: center;
  padding: 6rem 1.5rem 5rem;
  border-bottom: 1px solid var(--color-border);
}

.couple-name {
  font-family: var(--font-cursive);
  font-size: clamp(3.5rem, 12vw, 6.5rem);
  color: var(--color-gold);
  line-height: 1.1;
  margin-bottom: 0.25rem;
}

.event-title {
  font-size: 0.8rem;
  letter-spacing: 0.28em;
  text-transform: uppercase;
  color: var(--color-muted);
  margin-bottom: 1.75rem;
}

.event-details {
  font-size: 1rem;
  color: var(--color-olive);
  margin-bottom: 0.3rem;
}

.event-details .dot { margin: 0 0.5rem; color: var(--color-gold); }

.event-address {
  font-size: 0.9rem;
  color: var(--color-muted);
  margin-bottom: 2.5rem;
}

.invite-message {
  max-width: 620px;
  margin: 0 auto;
  font-size: 1.05rem;
  font-weight: 300;
  line-height: 1.85;
}

/* ── História ───────────────────────────────────── */
#historia {
  padding: var(--section-pad);
  background: var(--color-card-bg);
}

.two-col {
  display: flex;
  align-items: center;
  gap: 3rem;
}

.two-col.reverse { flex-direction: row-reverse; }

.section-photo {
  width: 260px;
  height: 320px;
  object-fit: cover;
  border-radius: var(--radius);
  border: 4px solid var(--color-border);
  flex-shrink: 0;
}

.section-text {
  font-size: 1.05rem;
  font-weight: 300;
  line-height: 1.9;
}

/* ── Trocadilho ─────────────────────────────────── */
#trocadilho {
  padding: var(--section-pad);
  background: var(--color-bg);
}

.trocadilho-text {
  font-size: 1.3rem;
  font-style: italic;
  font-weight: 300;
  line-height: 1.8;
  padding-left: 1.5rem;
  border-left: 3px solid var(--color-gold);
}

/* ── Programação ────────────────────────────────── */
#programacao {
  padding: var(--section-pad);
  background: var(--color-card-bg);
}

.programacao-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1.5rem;
  max-width: var(--max-w);
  margin: 2.5rem auto 0;
}

.programacao-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius);
  padding: 2rem 1.5rem;
  text-align: center;
}

.prog-icone   { font-size: 2.5rem; margin-bottom: 0.75rem; }
.prog-titulo  { font-weight: 700; font-size: 1rem; margin-bottom: 0.5rem; }
.prog-pun     { font-size: 0.88rem; color: var(--color-muted); font-style: italic; line-height: 1.6; }

/* ── Presentes ──────────────────────────────────── */
#presentes {
  padding: var(--section-pad);
  background: var(--color-bg);
}

.presentes-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 1.5rem;
  max-width: var(--max-w);
  margin: 0 auto;
}

.gift-card {
  background: var(--color-card-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius);
  overflow: hidden;
  transition: box-shadow 0.2s;
}

.gift-card:hover { box-shadow: 0 4px 16px rgba(0,0,0,0.08); }
.gift-card.reserved { opacity: 0.55; pointer-events: none; }

.gift-img {
  width: 100%;
  height: 200px;
  object-fit: cover;
  background: var(--color-border);
}

.gift-img-placeholder {
  width: 100%;
  height: 200px;
  background: var(--color-border);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-muted);
  font-size: 0.85rem;
}

.gift-body { padding: 1.25rem; }

.gift-name  { font-weight: 700; font-size: 1rem; margin-bottom: 0.25rem; }
.gift-desc  { font-size: 0.85rem; color: var(--color-muted); line-height: 1.5; margin-bottom: 0.75rem; }
.gift-price { font-size: 0.8rem; color: var(--color-gold); font-weight: 700; margin-bottom: 0.75rem; }

.color-dots {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 1rem;
}

.color-dot {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  border: 1.5px solid var(--color-border);
  flex-shrink: 0;
}

.btn-reserve {
  width: 100%;
  padding: 0.65rem;
  background: var(--color-olive);
  color: white;
  border-radius: 8px;
  font-size: 0.9rem;
  font-weight: 700;
  letter-spacing: 0.03em;
  transition: background 0.2s;
}

.btn-reserve:hover { background: var(--color-olive-light); }

.reserved-badge {
  display: none;
  width: 100%;
  padding: 0.65rem;
  background: var(--color-border);
  color: var(--color-muted);
  border-radius: 8px;
  font-size: 0.9rem;
  text-align: center;
}

.gift-card.reserved .btn-reserve   { display: none; }
.gift-card.reserved .reserved-badge { display: block; }

/* ── Pix ────────────────────────────────────────── */
#pix {
  padding: var(--section-pad);
  background: var(--color-card-bg);
}

.pix-container {
  display: flex;
  align-items: center;
  gap: 3rem;
  max-width: 560px;
  margin: 2rem auto 0;
}

.qrcode-placeholder {
  width: 160px;
  height: 160px;
  background: var(--color-border);
  border-radius: var(--radius);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.8rem;
  color: var(--color-muted);
  flex-shrink: 0;
}

.pix-info { flex: 1; }

.pix-label {
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-muted);
  margin-top: 1rem;
}

.pix-label:first-child { margin-top: 0; }

.pix-value {
  font-size: 1rem;
  font-weight: 700;
  color: var(--color-olive);
  margin-bottom: 0.25rem;
}

.btn-copy {
  margin-top: 1rem;
  padding: 0.6rem 1.5rem;
  background: var(--color-gold);
  color: white;
  border-radius: 8px;
  font-size: 0.9rem;
  font-weight: 700;
  transition: opacity 0.2s;
}

.btn-copy:hover { opacity: 0.85; }

.copy-feedback {
  margin-top: 0.5rem;
  font-size: 0.85rem;
  color: var(--color-olive);
}

/* ── Footer ─────────────────────────────────────── */
footer {
  text-align: center;
  padding: 3rem 1.5rem;
  border-top: 1px solid var(--color-border);
}

.footer-couple {
  font-family: var(--font-cursive);
  font-size: 2.2rem;
  color: var(--color-gold);
  margin-bottom: 0.5rem;
}

.footer-thanks {
  font-size: 0.9rem;
  color: var(--color-muted);
  font-style: italic;
}

/* ── Modal ──────────────────────────────────────── */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 1rem;
}

.modal {
  background: var(--color-card-bg);
  border-radius: var(--radius);
  padding: 2rem;
  max-width: 440px;
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
  position: relative;
}

.modal-close {
  position: absolute;
  top: 1rem;
  right: 1rem;
  font-size: 1.6rem;
  color: var(--color-muted);
  line-height: 1;
  padding: 0.2rem 0.5rem;
}

.modal-close:hover { color: var(--color-olive); }

.modal-gift-img {
  width: 100%;
  height: 200px;
  object-fit: cover;
  border-radius: 8px;
  margin-bottom: 1rem;
}

.modal-gift-name  { font-size: 1.2rem; font-weight: 700; margin-bottom: 0.25rem; }
.modal-gift-price { font-size: 0.85rem; color: var(--color-gold); font-weight: 700; margin-bottom: 1.5rem; }

.field-label {
  display: flex;
  flex-direction: column;
  font-size: 0.85rem;
  font-weight: 700;
  gap: 0.35rem;
  margin-bottom: 1rem;
}

.field-label input {
  padding: 0.65rem 0.85rem;
  border: 1.5px solid var(--color-border);
  border-radius: 8px;
  font-family: var(--font-body);
  font-size: 0.95rem;
  color: var(--color-olive);
  background: var(--color-bg);
  outline: none;
  transition: border-color 0.2s;
}

.field-label input:focus { border-color: var(--color-gold); }

.color-fieldset {
  border: none;
  margin-bottom: 1.25rem;
}

.color-fieldset legend {
  font-size: 0.85rem;
  font-weight: 700;
  margin-bottom: 0.75rem;
}

.color-options { display: flex; gap: 0.75rem; }

.color-circle {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  transition: transform 0.15s, box-shadow 0.15s;
  outline: none;
}

.color-circle:hover    { transform: scale(1.1); }
.color-circle.selected { box-shadow: 0 0 0 3px var(--color-gold); transform: scale(1.1); }

.form-error {
  font-size: 0.85rem;
  color: #c0392b;
  margin-bottom: 0.75rem;
}

.btn-primary {
  width: 100%;
  padding: 0.8rem;
  background: var(--color-olive);
  color: white;
  border-radius: 8px;
  font-size: 1rem;
  font-weight: 700;
  transition: background 0.2s;
}

.btn-primary:hover    { background: var(--color-olive-light); }
.btn-primary:disabled { background: var(--color-muted); cursor: not-allowed; }

.reservation-success { text-align: center; padding: 1rem 0; }
.success-emoji   { font-size: 3rem; margin-bottom: 1rem; }
.success-message { font-size: 1rem; line-height: 1.7; }

/* ── Responsive ─────────────────────────────────── */
@media (max-width: 640px) {
  .two-col,
  .two-col.reverse {
    flex-direction: column;
  }

  .section-photo {
    width: 100%;
    height: 260px;
  }

  .pix-container {
    flex-direction: column;
    text-align: center;
    gap: 1.5rem;
  }

  .modal { padding: 1.5rem; }
}
```

- [ ] **Step 2: Verify in browser**

Open `index.html`. Expected: off-white background, no console errors, Google Fonts loading (visible in DevTools Network). Page is still blank content-wise (sections are empty — that's correct at this stage).

- [ ] **Step 3: Commit**

```bash
git add styles.css
git commit -m "feat: add full CSS — palette, typography, layout, responsive"
```

---

### Task 3: config.js — all content

**Files:**
- Modify: `config.js`

- [ ] **Step 1: Write full CONFIG object**

```js
const CONFIG = {
  casal:   "Douglas & Keila",
  data:    "A definir",
  horario: "A definir",
  endereco: "A definir",

  mensagemConvite:
    "Cada casa guarda uma história. A nossa começa agora — " +
    "com novas paredes, novas raízes e o coração transbordando de gratidão. " +
    "Queremos dividir esse momento único com quem faz parte da nossa trajetória. " +
    "Venha celebrar conosco o início de um novo capítulo: o nosso lar.",

  historia:
    "Depois de sonhar, planejar e esperar, chegou a hora. " +
    "Douglas e Keila estão prontos para escrever uma nova história — agora com endereço fixo. " +
    "Esse cantinho é o reflexo de tudo que construímos juntos, " +
    "e não faria sentido inaugurá-lo sem as pessoas que amamos.",

  trocadilho:
    "Venha pro nosso chá… vai ter muita comida. " +
    "Sério, a gente planejou demais nisso.",

  programacao: [
    {
      icone: "🍽️",
      titulo: "Comer",
      pun: "Aqui não tem fome de amor — tem fome de comida mesmo. Prepara o estômago.",
    },
    {
      icone: "🎮",
      titulo: "Brincar",
      pun: "Vai ter brincadeira pra todo mundo. Spoiler: adulto vai sair chorando.",
    },
    {
      icone: "🎁",
      titulo: "Abrir presentes",
      pun: "Esse é o momento mais honesto do dia. " +
           "A gente abre, vocês filmam, e a gente finge que amou tudo.",
    },
  ],

  confirmacao:
    "Que alegria! Estamos ansiosos com a sua presença " +
    "e já contamos com você pra esse momento especial. 🏠",

  pix: {
    telefone: "(xx) xxxxx-xxxx",
    chave:    "xxxxxxxxxxx",
  },

  supabase: {
    url:      "SUA_URL_AQUI",
    anonKey:  "SUA_ANON_KEY_AQUI",
  },

  presentes: [
    {
      id:      "p1",
      nome:    "Jogo de panelas",
      descricao: "Conjunto completo para a nova cozinha",
      preco:   249.90,
      imagem:  "assets/presentes/panelas.jpg",
    },
    {
      id:      "p2",
      nome:    "Jogo de lençóis",
      descricao: "Para noites quentinhas no novo lar",
      preco:   149.90,
      imagem:  "assets/presentes/lencois.jpg",
    },
    {
      id:      "p3",
      nome:    "Conjunto de toalhas",
      descricao: "Kit com 4 toalhas de banho",
      preco:   129.90,
      imagem:  "assets/presentes/toalhas.jpg",
    },
  ],
}
```

- [ ] **Step 2: Verify CONFIG in browser console**

Open browser console on `index.html`.
Type: `CONFIG.casal`
Expected: `"Douglas & Keila"`

- [ ] **Step 3: Commit**

```bash
git add config.js
git commit -m "feat: add CONFIG with couple info, schedule, placeholder gifts and Pix"
```

---

### Task 4: app.js — render all sections + modal + Pix

**Files:**
- Modify: `app.js`

- [ ] **Step 1: Write full app.js**

```js
// ── Supabase client (credentials filled in Task 5) ──
const supabaseClient = window.supabase.createClient(
  CONFIG.supabase.url,
  CONFIG.supabase.anonKey
)

// ── State ──────────────────────────────────────────
const reservedIds    = new Set()
let   selectedGiftId = null

// ── Render ─────────────────────────────────────────

function renderHero() {
  document.getElementById('couple-name').textContent  = CONFIG.casal
  document.getElementById('event-date').textContent   = CONFIG.data
  document.getElementById('event-time').textContent   = CONFIG.horario
  document.getElementById('event-address').textContent = CONFIG.endereco
  document.getElementById('invite-message').textContent = CONFIG.mensagemConvite
}

function renderHistoria() {
  document.getElementById('historia-text').textContent = CONFIG.historia
}

function renderTrocadilho() {
  document.getElementById('trocadilho-text').textContent = CONFIG.trocadilho
}

function renderProgramacao() {
  document.getElementById('programacao-grid').innerHTML = CONFIG.programacao.map(item => `
    <div class="programacao-card">
      <div class="prog-icone">${item.icone}</div>
      <p class="prog-titulo">${item.titulo}</p>
      <p class="prog-pun">${item.pun}</p>
    </div>
  `).join('')
}

function renderPresentes() {
  document.getElementById('presentes-grid').innerHTML = CONFIG.presentes.map(gift => `
    <div class="gift-card" id="gift-${gift.id}">
      <img class="gift-img" src="${gift.imagem}" alt="${gift.nome}"
        onerror="this.outerHTML='<div class=\\'gift-img-placeholder\\'>${gift.nome}</div>'">
      <div class="gift-body">
        <p class="gift-name">${gift.nome}</p>
        <p class="gift-desc">${gift.descricao}</p>
        <p class="gift-price">Sugestão: R$ ${gift.preco.toFixed(2).replace('.', ',')}</p>
        <div class="color-dots">
          <div class="color-dot" style="background:#D3D3D3" title="Cinza claro"></div>
          <div class="color-dot" style="background:#8D8D8D" title="Inox"></div>
          <div class="color-dot" style="background:#FAF8F5" title="Off-white"></div>
        </div>
        <button class="btn-reserve" data-id="${gift.id}">Quero presentear</button>
        <div class="reserved-badge">Já reservado 🎁</div>
      </div>
    </div>
  `).join('')
}

function renderPix() {
  document.getElementById('pix-phone').textContent = CONFIG.pix.telefone
  document.getElementById('pix-key').textContent   = CONFIG.pix.chave
}

function renderFooter() {
  document.getElementById('footer-couple').textContent = CONFIG.casal
}

// ── Modal ──────────────────────────────────────────

function openModal(gift) {
  selectedGiftId = gift.id

  const img = document.getElementById('modal-gift-img')
  img.src = gift.imagem
  img.alt = gift.nome

  document.getElementById('modal-gift-name').textContent  = gift.nome
  document.getElementById('modal-gift-price').textContent =
    `Sugestão: R$ ${gift.preco.toFixed(2).replace('.', ',')}`

  document.getElementById('reservation-form').classList.remove('hidden')
  document.getElementById('reservation-success').classList.add('hidden')
  document.getElementById('form-error').classList.add('hidden')
  document.getElementById('field-name').value     = ''
  document.getElementById('field-whatsapp').value = ''
  document.getElementById('field-color').value    = ''
  document.querySelectorAll('.color-circle').forEach(c => c.classList.remove('selected'))

  document.getElementById('modal-overlay').classList.remove('hidden')
  document.body.style.overflow = 'hidden'
}

function closeModal() {
  document.getElementById('modal-overlay').classList.add('hidden')
  document.body.style.overflow = ''
  selectedGiftId = null
}

// ── Helpers ────────────────────────────────────────

function maskWhatsApp(value) {
  const d = value.replace(/\D/g, '').slice(0, 11)
  if (d.length <= 2) return `(${d}`
  if (d.length <= 7) return `(${d.slice(0, 2)}) ${d.slice(2)}`
  return `(${d.slice(0, 2)}) ${d.slice(2, 7)}-${d.slice(7)}`
}

function markGiftAsReserved(giftId) {
  reservedIds.add(giftId)
  const card = document.getElementById(`gift-${giftId}`)
  if (card) card.classList.add('reserved')
}

function copyPixKey() {
  navigator.clipboard.writeText(CONFIG.pix.chave).then(() => {
    const fb = document.getElementById('copy-feedback')
    fb.classList.remove('hidden')
    setTimeout(() => fb.classList.add('hidden'), 2000)
  })
}

// ── Supabase ───────────────────────────────────────

async function loadReservedGifts() {
  const { data, error } = await supabaseClient.from('reservas').select('presente_id')
  if (error) { console.error('Erro ao carregar reservas:', error); return }
  data.forEach(row => markGiftAsReserved(row.presente_id))
}

function subscribeToReservations() {
  supabaseClient
    .channel('reservas-changes')
    .on(
      'postgres_changes',
      { event: 'INSERT', schema: 'public', table: 'reservas' },
      payload => markGiftAsReserved(payload.new.presente_id)
    )
    .subscribe()
}

async function submitReservation() {
  const name     = document.getElementById('field-name').value.trim()
  const whatsapp = document.getElementById('field-whatsapp').value.trim()
  const color    = document.getElementById('field-color').value
  const errorEl  = document.getElementById('form-error')

  errorEl.classList.add('hidden')

  if (!name || !whatsapp || !color) {
    errorEl.textContent = 'Preencha todos os campos, incluindo a cor.'
    errorEl.classList.remove('hidden')
    return
  }

  if (whatsapp.replace(/\D/g, '').length < 10) {
    errorEl.textContent = 'WhatsApp inválido. Use o formato (11) 99999-9999.'
    errorEl.classList.remove('hidden')
    return
  }

  const btn = document.getElementById('btn-submit')
  btn.disabled    = true
  btn.textContent = 'Confirmando...'

  const { error } = await supabaseClient.from('reservas').insert({
    presente_id:    selectedGiftId,
    nome_convidado: name,
    whatsapp:       whatsapp,
    cor_escolhida:  color,
  })

  btn.disabled    = false
  btn.textContent = 'Confirmar reserva'

  if (error) {
    errorEl.textContent = error.code === '23505'
      ? 'Ops! Esse presente acabou de ser reservado por outra pessoa.'
      : 'Erro ao confirmar. Tente novamente.'
    if (error.code !== '23505') console.error(error)
    errorEl.classList.remove('hidden')
    return
  }

  document.getElementById('reservation-form').classList.add('hidden')
  document.getElementById('success-message').textContent = CONFIG.confirmacao
  document.getElementById('reservation-success').classList.remove('hidden')
  setTimeout(closeModal, 4500)
}

// ── Event listeners ────────────────────────────────

function setupEventListeners() {
  document.getElementById('presentes-grid').addEventListener('click', e => {
    const btn = e.target.closest('.btn-reserve')
    if (!btn) return
    const gift = CONFIG.presentes.find(g => g.id === btn.dataset.id)
    if (gift) openModal(gift)
  })

  document.getElementById('modal-close').addEventListener('click', closeModal)
  document.getElementById('modal-overlay').addEventListener('click', e => {
    if (e.target.id === 'modal-overlay') closeModal()
  })

  document.getElementById('color-options').addEventListener('click', e => {
    const circle = e.target.closest('.color-circle')
    if (!circle) return
    document.querySelectorAll('.color-circle').forEach(c => c.classList.remove('selected'))
    circle.classList.add('selected')
    document.getElementById('field-color').value = circle.dataset.color
  })

  document.getElementById('field-whatsapp').addEventListener('input', e => {
    e.target.value = maskWhatsApp(e.target.value)
  })

  document.getElementById('reservation-form').addEventListener('submit', e => {
    e.preventDefault()
    submitReservation()
  })

  document.getElementById('btn-copy-pix').addEventListener('click', copyPixKey)
}

// ── Init ───────────────────────────────────────────

async function init() {
  renderHero()
  renderHistoria()
  renderTrocadilho()
  renderProgramacao()
  renderPresentes()
  renderPix()
  renderFooter()
  setupEventListeners()
  await loadReservedGifts()
  subscribeToReservations()
}

init()
```

- [ ] **Step 2: Verify in browser — all sections visible**

Open `index.html`. Expected:
- "Douglas & Keila" in gold cursive in hero
- Invite message visible below date/address placeholders
- História and Trocadilho sections show text (photos hidden if files missing)
- 3 programação cards visible with puns
- 3 gift cards with placeholder background where images are missing, price, color dots, "Quero presentear" button

- [ ] **Step 3: Verify modal**

Click "Quero presentear" on any card.
Expected: modal opens with gift name and price. × closes it. Clicking backdrop closes it.

- [ ] **Step 4: Verify WhatsApp mask**

In open modal, type digits in the WhatsApp field.
Expected: formats automatically as `(11) 99999-9999`.

- [ ] **Step 5: Verify color circles**

Click each color circle.
Expected: gold ring appears on selected circle, others deselected.

- [ ] **Step 6: Verify Pix copy**

Click "Copiar chave".
Expected: "✓ Copiado!" appears for 2 seconds. Clipboard contains `xxxxxxxxxxx`.

- [ ] **Step 7: Verify form validation**

Submit the form with empty fields.
Expected: error message "Preencha todos os campos, incluindo a cor."

- [ ] **Step 8: Commit**

```bash
git add app.js
git commit -m "feat: render all sections from config, modal, form validation, Pix copy"
```

---

### Task 5: Supabase — database setup and credentials

**Files:**
- Modify: `config.js`

- [ ] **Step 1: Create Supabase project**

1. Go to supabase.com → New project
2. Note the **Project URL** and **anon public key** from Project Settings → API

- [ ] **Step 2: Run SQL in Supabase SQL Editor**

```sql
CREATE TABLE reservas (
  id              UUID        DEFAULT gen_random_uuid() PRIMARY KEY,
  presente_id     TEXT        NOT NULL UNIQUE,
  nome_convidado  TEXT        NOT NULL,
  whatsapp        TEXT        NOT NULL,
  cor_escolhida   TEXT        NOT NULL
    CHECK (cor_escolhida IN ('cinza-claro', 'inox', 'off-white')),
  reservado_em    TIMESTAMPTZ DEFAULT now()
);

ALTER TABLE reservas ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can view reservas"
  ON reservas FOR SELECT USING (true);

CREATE POLICY "Anyone can insert reservas"
  ON reservas FOR INSERT WITH CHECK (true);

ALTER PUBLICATION supabase_realtime ADD TABLE reservas;
```

- [ ] **Step 3: Update credentials in config.js**

Replace the supabase block:
```js
supabase: {
  url:     "https://xxxxxxxxxxx.supabase.co",
  anonKey: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
},
```

- [ ] **Step 4: Verify connection in browser console**

Open `index.html`, open DevTools console and run:
```js
const { data, error } = await supabaseClient.from('reservas').select('*')
console.log(data, error)
```
Expected: `[]` and `null`.

- [ ] **Step 5: Commit**

```bash
git add config.js
git commit -m "feat: add Supabase credentials — database ready"
```

---

### Task 6: Verify Supabase integration end-to-end

**Files:** none (testing only)

- [ ] **Step 1: Verify load of reserved gifts**

In Supabase Dashboard → Table Editor → `reservas`, insert a row manually:
- `presente_id`: `p1`
- `nome_convidado`: `Teste`
- `whatsapp`: `(11) 11111-1111`
- `cor_escolhida`: `inox`

Reload `index.html`.
Expected: first gift card shows "Já reservado 🎁" badge with button gone and card dimmed.

- [ ] **Step 2: Verify Realtime**

Open `index.html` in **two** browser tabs simultaneously.
In Supabase Dashboard, insert a row for `p2`.
Expected: **both tabs** update — second gift shows "Já reservado" without any reload.

- [ ] **Step 3: Verify reservation form happy path**

Delete all test rows from Supabase Dashboard.
Reload page — all gifts available again.
Click "Quero presentear" on gift p1.
Fill: name = `Maria Teste`, WhatsApp = `(11) 99999-8888`, color = `inox`.
Click "Confirmar reserva".
Expected:
- Button shows "Confirmando..." briefly
- Form hides, success message appears with CONFIG.confirmacao text
- Modal closes after ~4.5 seconds
- Gift p1 card shows "Já reservado"
- Row visible in Supabase Dashboard

- [ ] **Step 4: Verify duplicate prevention**

Try to reserve p1 again (open modal via another tab before it marks as reserved).
Expected: error "Ops! Esse presente acabou de ser reservado por outra pessoa."

- [ ] **Step 5: Clean up and commit**

Delete test rows from Supabase Dashboard.

```bash
git commit --allow-empty -m "test: verify Supabase integration end-to-end"
```

---

### Task 7: GitHub Pages deploy

**Files:** none new

- [ ] **Step 1: Create GitHub repository**

```bash
# On GitHub: create a new empty repo named "casa-nova"
# Then locally:
git remote add origin https://github.com/<seu-usuario>/casa-nova.git
git push -u origin master
```

- [ ] **Step 2: Enable GitHub Pages**

1. Go to repository → Settings → Pages
2. Source: `Deploy from a branch`
3. Branch: `master`, folder: `/ (root)`
4. Click Save

- [ ] **Step 3: Verify deploy**

Wait ~2 minutes, open `https://<seu-usuario>.github.io/casa-nova`.
Expected: full site loads, Supabase works, gift reservation works end-to-end.

---

## After launch — adding real content

When you have real photos and gift data, update these files only:

**To add real gifts** — edit `config.js` `presentes` array (add images to `assets/presentes/`):
```js
{ id: "p4", nome: "Nome do presente", descricao: "...", preco: 199.90, imagem: "assets/presentes/nome.jpg" }
```

**To update event details** — edit `config.js` top section:
```js
data: "12 de Julho de 2026",
horario: "15h",
endereco: "Rua das Flores, 123 — Bairro, Cidade",
```

**To update Pix** — edit `config.js` `pix` block with real values.

**To add more programação items** — add to `config.js` `programacao` array:
```js
{ icone: "🎶", titulo: "Música", pun: "Tem playlist, tem dança, tem vergonha alheia." }
```

After any change: `git add config.js assets/ && git commit -m "content: update gifts" && git push`
